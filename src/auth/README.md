# auth

`auth` 是鉴权与商业化模块，MVP 阶段暂不实现。

## 未来职责

- 用户登录。
- 登出。
- Token 刷新。
- 本地安全存储。
- 用户套餐查询。
- 配额缓存。
- Agent 执行前的配额校验。

## 设计原则

鉴权和配额不应该写进 Agent Loop。

推荐流程：

```text
用户提交任务
-> quota guard 检查
-> 通过后调用 AgentLoop
```

而不是：

```text
AgentLoop 内部判断用户套餐
```

## MVP 状态

第一期不做登录和配额，默认本地单用户使用。
