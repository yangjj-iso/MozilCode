[project]
name = "mozilcode-workspace"
version = "0.1.0"
description = "Workspace metadata for the MozilCode project."
requires-python = ">=3.11"
dependencies = []

[tool.uv]
package = false

[tool.uv.workspace]
members = ["MozilCode-python"]
